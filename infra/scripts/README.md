# infra/scripts

Scripts operativos de despliegue (respaldo/restauración de PostgreSQL y
MinIO, verificación post-deploy). Se agregan en la Fase 8 junto con
`docs/deployment.md`. Por ahora este directorio queda reservado para no
reorganizar la estructura del monorepo más adelante.
